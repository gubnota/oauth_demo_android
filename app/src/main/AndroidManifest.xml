<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OAuth2AndroidExample"
        android:usesCleartextTraffic="true">
        <!-- Login Activity with WebView -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <!-- Success Activity -->
        <activity
            android:name=".SuccessActivity"
            android:exported="false" />

        <!-- Deep Link Handler Activity -->
        <!-- This activity handles the OAuth2 redirect callback -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Custom scheme for OAuth2 callback -->
                <!-- Format: oauth2demo://callback -->
                <data
                    android:scheme="oauth2demo"
                    android:host="callback" />
            </intent-filter>
        </activity>

    </application>

</manifest>
